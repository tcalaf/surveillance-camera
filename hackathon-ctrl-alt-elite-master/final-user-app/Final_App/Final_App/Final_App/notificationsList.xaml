<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             BackgroundColor="#e9c46a"
             x:Class="Final_App.notificationsList">

    <StackLayout Opacity="100">
        <ListView x:Name="tasksListView"  HasUnevenRows="true" IsEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Height="65" x:Name="viewCelle" >
                        <StackLayout Orientation="Horizontal">
                            <Label x:Name="textcelula" Text="{Binding Name}" TextColor="#264653" FontSize="20" VerticalOptions ="CenterAndExpand" HorizontalOptions="CenterAndExpand" WidthRequest="400" Opacity="10" InputTransparent="True" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>